{
  "name": "startup-advisor-board",
  "version": "0.1.0",
  "description": "A panel of specialist LLM agents that critique and plan a startup idea. Host-agnostic prompts + per-host build scripts.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run build:claude-code && npm run build:codex",
    "build:claude-code": "node hosts/claude-code/build.mjs",
    "build:codex": "node hosts/codex/build.mjs",
    "build:kiro": "node hosts/kiro/build.mjs",
    "clean": "rm -rf dist"
  },
  "license": "MIT"
}
